# backend-python/.env

# Your API key for Gemini (from Google AI Studio or Google Cloud)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Optional: Specify exact Gemini models if you prefer
GEMINI_VISION_MODEL=gemini-pro-vision
GEMINI_TEXT_MODEL=gemini-pro

# Your API key for Google Maps Static API (from Google Cloud Console)
# This key needs to have the "Maps Static API" enabled.
Maps_STATIC_API_KEY=AIzadfwefger

# Database connection string (e.g., for PostgreSQL) - Primary DB interaction will be via Node.js,
# but Python could still connect for data seeding or specific lookups if needed.
# DATABASE_URL="postgresql://user:password@host:port/dbname"